html************************************
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Objects</title>
        <style>
            #balpar{
                background-color: #76bc44 ; 
                font-size: 20px;
                font-family: cursive;
            }
            
        </style>
    
    </head>
    
    <body>
<!--below we use the span tags to test if certain content is found within a string. -->
        <p>String: <span id="string"></span></p>
        <p>Pattern: <span id="pattern"></span></p>
        <p>Result(Search): <span id="search"></span></p>
        <p>Result(Match): <span id="match"></span></p>
        <p>Result(Test): <span id="test"></span></p>

        <script type="text/javascript" src="code.js">
        </script>
    
    </body>


</html>

JS****************************************************
//below we created a variable of type string and then utilized the pattern keyword and set it equal to a/g this means we are searching 
for the letter a and that we are searching globally. the search method finds the place holder of the first a and since we have to 
declared it globally it is searching for the first lower case a. the match function finds and displays how many match our search and 
the test will return a true or false.
var str = "Atlanta";
var pattern = /a/g;
document.getElementById("string").innerHTML = str;
document.getElementById("pattern").innerHTML = pattern;
document.getElementById("search").innerHTML = str.search(pattern);
document.getElementById("match").innerHTML = str.match(pattern);
document.getElementById("test").innerHTML = pattern.test(search);


//below is a way to find numbers within a string
var str = "Atlanta 6512478523641";
var pattern2 = /[4-7]/g;
document.getElementById("string").innerHTML = str;
document.getElementById("pattern").innerHTML = pattern2;
document.getElementById("search").innerHTML = str.search(pattern2);
document.getElementById("match").innerHTML = str.match(pattern2);
document.getElementById("test").innerHTML = pattern2.test(search);
